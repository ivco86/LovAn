# Пълен списък с всички функции в AI Gallery приложението

## 📋 Съдържание
1. [API Endpoints (Backend)](#api-endpoints-backend)
2. [JavaScript функции (Frontend)](#javascript-функции-frontend)
3. [Основни системни функции](#основни-системни-функции)
4. [UI функционалности](#ui-функционалности)

---

## 🔌 API Endpoints (Backend)

### Система (System)
- `GET /api/health` - Проверка на здравословното състояние на системата и AI връзката
- `GET /api/config` - Вземане на текущата конфигурация
- `GET /api/ai/styles` - Вземане на наличните AI стилове за описания
- `GET /api/external-apps` - Списък с външни приложения за отваряне на изображения/видеа
- `POST /api/settings/external-apps` - Добавяне на ново външно приложение
- `PUT /api/settings/external-apps/<media_type>/<app_id>` - Актуализиране на външно приложение
- `DELETE /api/settings/external-apps/<media_type>/<app_id>` - Изтриване на външно приложение
- `POST /api/images/<int:image_id>/open-with` - Отваряне на изображение/видео с външно приложение

### Изображения (Images)

#### CRUD операции
- `GET /api/images` - Списък с всички изображения (с филтри)
- `GET /api/images/<int:image_id>` - Детайли за конкретно изображение
- `PATCH /api/images/<int:image_id>` - Актуализиране на изображение (описание, тагове)

#### Файлове
- `GET /api/images/<int:image_id>/file` - Подаване на оригиналния файл на изображението
- `GET /api/images/<int:image_id>/thumbnail` - Подаване на миниатюра

#### Операции
- `POST /api/images/<int:image_id>/favorite` - Превключване на любимо изображение
- `POST /api/images/<int:image_id>/rename` - Преименуване на файл
- `POST /api/images/<int:image_id>/open-folder` - Отваряне на папката на изображението

#### Анализ и AI
- `POST /api/images/<int:image_id>/analyze` - Анализиране на изображение с AI
- `POST /api/analyze-batch` - Пакетно анализиране на множество изображения

#### Търсене
- `GET /api/images/search` - Търсене на изображения по текст
- `GET /api/images/<int:image_id>/similar` - Намиране на подобни изображения (CLIP)
- `GET /api/images/<int:image_id>/reverse-search` - Опции за обратно търсене на изображения

#### Тагове
- `GET /api/tags` - Списък с всички тагове и техния брой
- `GET /api/tags/suggestions` - Предложения за тагове
- `GET /api/tags/<tag>/related` - Свързани тагове

### Пътеки/Дъски (Boards)
- `GET /api/boards` - Списък с всички дъски (иерархично)
- `POST /api/boards` - Създаване на нова дъска
- `GET /api/boards/<int:board_id>` - Детайли за дъска с изображения
- `PUT /api/boards/<int:board_id>` - Актуализиране на дъска
- `DELETE /api/boards/<int:board_id>` - Изтриване на дъска
- `POST /api/boards/<int:board_id>/images` - Добавяне на изображение към дъска
- `DELETE /api/boards/<int:board_id>/images` - Премахване на изображение от дъска
- `PUT /api/boards/<int:board_id>/smart-rules` - Актуализиране на smart rules за дъска
- `POST /api/boards/<int:board_id>/process-smart` - Обработка на всички изображения според smart rules
- `POST /api/boards/<int:board_id>/merge` - Сливане на дъска с друга дъска

### Лица и Хора (Faces & People)
- `POST /api/faces/detect/<int:image_id>` - Откриване на лица в изображение
- `GET /api/faces/image/<int:image_id>` - Всички лица, открити в изображение
- `GET /api/people` - Списък с всички групи от хора
- `GET /api/people/<int:person_id>` - Детайли за човек с лицата му
- `POST /api/people` - Създаване на нова група от хора
- `PUT /api/people/<int:person_id>` - Актуализиране на име на човек
- `DELETE /api/people/<int:person_id>` - Изтриване на група от хора
- `POST /api/faces/<int:face_id>/assign` - Присвояване на лице към човек
- `POST /api/faces/<int:face_id>/unassign` - Премахване на лице от човек
- `GET /api/faces/unassigned` - Всички лица, които не са присвоени
- `POST /api/faces/cluster` - Автоматично групиране на подобни лица
- `GET /api/faces/status` - Статус на услугата за разпознаване на лица

### EXIF метаданни
- `GET /api/images/<int:image_id>/exif` - Вземане на EXIF данни за изображение
- `POST /api/images/<int:image_id>/exif/extract` - Извличане на EXIF данни от файл
- `POST /api/images/search/exif` - Търсене на изображения по EXIF критерии
- `GET /api/exif/cameras` - Списък с всички камери

### Embeddings & Semantic Search
- `GET /api/embeddings/status` - Статус на embeddings услугата
- `POST /api/embeddings/generate` - Генериране на embeddings за изображения
- `POST /api/search/semantic` - Семантично търсене на изображения
- `GET /api/images/<int:image_id>/similar` - Подобни изображения чрез embeddings

### Импорт
- `POST /api/scan` - Сканиране на директория за нови изображения
- `POST /api/upload` - Качване на изображения/видеа

### Експорт
- `POST /api/export/images/csv` - Експорт на метаданни в CSV формат
- `POST /api/export/images/json` - Експорт на метаданни в JSON формат
- `POST /api/export/images/pdf` - Генериране на PDF каталог от избрани изображения
- `GET /api/export/boards/<int:board_id>/csv` - Експорт на дъска в CSV
- `GET /api/export/boards/<int:board_id>/json` - Експорт на дъска в JSON
- `POST /api/export/boards/<int:board_id>/pdf` - Генериране на PDF каталог за дъска

### Telegram Bot
- `GET /api/telegram/status` - Статус на Telegram бот
- `POST /api/telegram/start` - Стартиране на Telegram бот
- `POST /api/telegram/stop` - Спиране на Telegram бот
- `GET /api/telegram/config` - Вземане на конфигурация на бот
- `POST /api/telegram/config` - Актуализиране на конфигурация на бот
- `GET /api/telegram/logs` - Логове на Telegram бот
- `POST /api/telegram/send-photo` - Изпращане на изображение към Telegram чат

---

## 💻 JavaScript функции (Frontend)

### Инициализация
- `initializeApp()` - Инициализация на приложението
- `checkHealth()` - Проверка на здравословното състояние на системата
- `attachEventListeners()` - Прикачване на event listeners

### Зареждане на данни
- `loadImages(filters)` - Зареждане на изображения с филтри
- `loadBoards()` - Зареждане на дъски
- `loadTags()` - Зареждане на тагове
- `loadAIStyles()` - Зареждане на AI стилове
- `loadExternalApps()` - Зареждане на външни приложения
- `updateStats()` - Актуализиране на статистики
- `loadPeople()` - Зареждане на хора/лица
- `loadUnknownFaces()` - Зареждане на неразпознати лица

### Рендериране (Rendering)
- `renderImages()` - Рендериране на изображения в галерия
- `renderBoards()` - Рендериране на дъски в sidebar
- `renderTagCloud()` - Рендериране на таговете
- `renderPeopleList(people)` - Рендериране на хората като тагове
- `createImageCard(image)` - Създаване на карта за изображение
- `createBoardItem(board, isSubBoard)` - Създаване на елемент за дъска
- `renderSimilarImagesInContainer(container, similarImages)` - Рендериране на подобни изображения
- `renderEditTags()` - Рендериране на таговете при редактиране
- `updateCounts()` - Актуализиране на броячите
- `updateBreadcrumb(text)` - Актуализиране на breadcrumb

### Изображения
- `openImageModal(image)` - Отваряне на модал с детайли за изображение
- `closeImageModal()` - Затваряне на модал за изображение
- `updateModal()` - Актуализиране на модал с данни
- `getImageDetails(imageId)` - Вземане на детайли за изображение
- `toggleFavorite(imageId)` - Превключване на любимо изображение
- `renameImage(imageId, newFilename)` - Преименуване на изображение
- `editImage(imageId)` - Редактиране на изображение
- `saveImageEdit()` - Запазване на редакциите на изображение
- `openImageFolder(imageId)` - Отваряне на папката на изображение
- `openWithApp(imageId, appId)` - Отваряне на изображение с външно приложение
- `showOpenWithMenu(event, imageId, mediaType)` - Показване на меню за отваряне с приложение

### Анализ и AI
- `analyzeImage(imageId, style, customPrompt)` - Анализиране на изображение с AI
- `batchAnalyze(limit)` - Пакетно анализиране
- `batchAnalyzeImages()` - Пакетно анализиране на избрани изображения
- `performBatchAnalyze(style, customPrompt)` - Изпълнение на пакетно анализиране
- `openAIStyleModal(imageId, isBatchMode)` - Отваряне на модал за избор на AI стил
- `renderAIStylesModal()` - Рендериране на модал за AI стилове
- `closeAIStyleModal()` - Затваряне на модал за AI стилове

### Търсене
- `searchImages(query)` - Търсене на изображения
- `searchByTag(tag)` - Търсене по таг
- `loadSimilarImages(imageId)` - Зареждане на подобни изображения
- `openReverseSearchModal(imageId)` - Отваряне на модал за обратно търсене
- `openSearchAndDownload(searchUrl, imageUrl, filename, provider)` - Отваряне на търсене и изтегляне

### Дъски (Boards)
- `createBoard(name, description, parentId)` - Създаване на нова дъска
- `loadBoard(boardId)` - Зареждане на дъска
- `addImageToBoard(boardId, imageId)` - Добавяне на изображение към дъска
- `removeImageFromBoard(boardId, imageId)` - Премахване на изображение от дъска
- `renameBoard(boardId, newName, newDescription)` - Преименуване на дъска
- `deleteBoard(boardId, deleteSubBoards)` - Изтриване на дъска
- `mergeBoards(sourceBoardId, targetBoardId, deleteSource)` - Сливане на дъски
- `openCreateBoardModal()` - Отваряне на модал за създаване на дъска
- `closeCreateBoardModal()` - Затваряне на модал за създаване на дъска
- `openAddToBoardModal(imageId, isBatchMode)` - Отваряне на модал за добавяне към дъска
- `closeAddToBoardModal()` - Затваряне на модал за добавяне към дъска
- `openRenameBoardModal(boardId)` - Отваряне на модал за преименуване на дъска
- `closeRenameBoardModal()` - Затваряне на модал за преименуване
- `openMergeBoardModal(boardId)` - Отваряне на модал за сливане на дъски
- `closeMergeBoardModal()` - Затваряне на модал за сливане
- `openDeleteBoardModal(boardId)` - Отваряне на модал за изтриване на дъска
- `closeDeleteBoardModal()` - Затваряне на модал за изтриване
- `openSmartRulesModal(boardId)` - Отваряне на модал за smart rules
- `closeSmartRulesModal()` - Затваряне на модал за smart rules
- `findBoardById(boardId, boards)` - Намиране на дъска по ID
- `getAllSubBoardIds(boardId, boards)` - Вземане на всички поддъски
- `showBoardContextMenu(boardId, x, y)` - Показване на контекстно меню за дъска
- `hideBoardContextMenu()` - Скриване на контекстно меню

### Лица и Хора (Faces & People)
- `togglePeopleSection()` - Превключване на секцията за хора
- `openPeopleModal()` - Отваряне на модал за всички хора
- `renderPeopleModal(people)` - Рендериране на хора в модал
- `openPersonDetail(personId)` - Отваряне на детайли за човек
- `openPersonDetailForEdit(personId)` - Отваряне на детайли за редактиране
- `savePersonName()` - Запазване на име на човек
- `deletePerson()` - Изтриване на човек
- `clusterFaces()` - Автоматично групиране на лица
- `showPeopleView()` - Показване на изглед с всички хора
- `showPersonImages(personId, personName)` - Показване на изображения за човек
- `removeFaceFromPerson(faceId, personId)` - Премахване на лице от човек
- `renderUnknownFaces(faces)` - Рендериране на неразпознати лица
- `openAssignFaceModal(faceId, imageId)` - Отваряне на модал за присвояване на лице
- `renderInlinePersonSuggestions(people, searchTerm)` - Рендериране на предложения за хора
- `assignFaceToPerson(faceId, personId, personName)` - Присвояване на лице към човек
- `createNewPersonFromInline(name)` - Създаване на нов човек от inline форма
- `filterInlinePersonSearch()` - Филтриране на търсенето за хора
- `cancelAssign()` - Отказване от присвояване
- `initFacesUI()` - Инициализация на UI за лица

### Изгледи (Views)
- `switchView(view, param)` - Превключване на изглед (all, favorites, unanalyzed, videos, board, person)

### Импорт и качване
- `scanDirectory()` - Сканиране на директория
- `openUploadModal()` - Отваряне на модал за качване
- `closeUploadModal()` - Затваряне на модал за качване
- `handleFileSelect(e)` - Обработка на избор на файлове
- `showUploadPreview()` - Показване на преглед на файлове за качване
- `populateUploadBoardSelect()` - Попълване на селекция за дъска при качване
- `removeUploadFile(index)` - Премахване на файл от списъка за качване
- `uploadFiles()` - Качване на файлове

### Селекция и пакетни операции
- `toggleSelectionMode()` - Превключване на режим за селекция
- `toggleImageSelection(imageId)` - Превключване на селекция на изображение
- `selectAllImages()` - Селектиране на всички изображения
- `deselectAllImages()` - Премахване на селекция от всички изображения
- `updateSelectionUI()` - Актуализиране на UI за селекция
- `batchTagImages()` - Пакетно добавяне на тагове
- `batchNameImages()` - Пакетно преименуване на изображения
- `batchAddImagesToBoard()` - Пакетно добавяне на изображения към дъска

### Експорт
- `exportSingleImage(imageId, format)` - Експорт на единично изображение
- `exportSelectedImages(format)` - Експорт на избрани изображения
- `exportBoard(boardId, format)` - Експорт на дъска
- `generateBoardPDF(boardId)` - Генериране на PDF за дъска
- `showExportMenu(event, imageId)` - Показване на меню за експорт
- `showBatchExportMenu(event)` - Показване на меню за пакетен експорт
- `showBoardExportMenu(event, boardId)` - Показване на меню за експорт на дъска
- `showBoardPDFOptions(boardId)` - Показване на опции за PDF за дъска

### Дубликати
- `findDuplicates()` - Намиране на дублирани изображения
- `createDuplicateGroup(images, groupIndex)` - Създаване на група дубликати
- `createDuplicateCard(image)` - Създаване на карта за дубликат
- `closeDuplicatesModal()` - Затваряне на модал за дубликати

### Сортиране
- `sortImages(images, sortType)` - Сортиране на изображения (по дата, име, размер)

### Telegram
- `loadBotStatus()` - Зареждане на статус на бот
- `updateBotStatusUI(status)` - Актуализиране на UI за статус на бот
- `startBot()` - Стартиране на Telegram бот
- `stopBot()` - Спиране на Telegram бот
- `testBot()` - Тестване на Telegram бот
- `viewBotLogs()` - Преглед на логове на бот
- `loadBotLogs()` - Зареждане на логове на бот
- `loadBotConfig()` - Зареждане на конфигурация на бот
- `saveBotConfig()` - Запазване на конфигурация на бот
- `openSendToTelegramModal(imageId)` - Отваряне на модал за изпращане към Telegram
- `sendPhotoToTelegram(imageId)` - Изпращане на изображение към Telegram

### Настройки
- `openSettingsModal()` - Отваряне на модал за настройки
- `closeSettingsModal()` - Затваряне на модал за настройки
- `loadExternalAppsForSettings()` - Зареждане на външни приложения за настройки
- `renderAppsList(mediaType, apps)` - Рендериране на списък с приложения
- `showAddAppModal(mediaType)` - Показване на модал за добавяне на приложение
- `editApp(mediaType, appId)` - Редактиране на приложение
- `deleteApp(mediaType, appId)` - Изтриване на приложение

### Утилити
- `escapeHtml(text)` - Екраниране на HTML
- `formatFileSize(bytes)` - Форматиране на размер на файл
- `getConsistentHeight(imageId, width, height)` - Изчисляване на консистентна височина
- `showLoading()` - Показване на индикатор за зареждане
- `hideLoading()` - Скриване на индикатор за зареждане
- `showToast(message, type, duration)` - Показване на toast нотификация
- `closeModal(modalId, resetCallback)` - Затваряне на модал
- `flattenBoards(boards, prefix)` - Правене на дъските в плосък списък
- `getAllBoardsFlat()` - Вземане на всички дъски като плосък списък

---

## 🛠️ Основни системни функции

### База данни (Database)
- Управление на изображения (CRUD операции)
- Управление на дъски (CRUD операции, йерархия)
- Управление на лица и хора
- Управление на тагове и метаданни
- Управление на embeddings
- Smart rules за автоматично категоризиране

### AI услуги
- Генериране на описания за изображения
- Извличане на тагове
- Разпознаване на лица
- Генериране на embeddings за семантично търсене
- Анализ на EXIF метаданни

### Файлова система
- Генериране на миниатюри
- Управление на файлове
- Сканиране на директории

### Експорт
- CSV експорт
- JSON експорт
- PDF генерация с каталози

---

## 🎨 UI функционалности

### Галерия
- Grid изглед на изображения
- Modal за детайли на изображение
- Hover ефекти и превю
- Lazy loading на изображения

### Sidebar
- Навигация по изгледи (All, Favorites, Unanalyzed, Videos)
- Дъски с йерархична структура
- Хора като тагове
- Популярни тагове
- Статистики

### Модали
- Модал за детайли на изображение
- Модал за редактиране
- Модал за AI анализ
- Модал за дъски
- Модал за хора
- Модал за настройки
- Модал за качване
- Модал за експорт

### Пакетни операции
- Селекция на множество изображения
- Пакетно анализиране
- Пакетно добавяне на тагове
- Пакетно преименуване
- Пакетно добавяне към дъски
- Пакетен експорт

### Търсене и филтриране
- Текст търсене
- Търсене по тагове
- Семантично търсене
- Търсене по EXIF
- Обратно търсене на изображения

### Сортиране
- По дата (нов/стар)
- По име (A-Z / Z-A)
- По размер (голям/малък)

---

## 📊 Статистики

**Общо API endpoints:** ~70+
**Общо JavaScript функции:** ~120+
**Основни модули:** 8 (System, Images, Boards, Faces, Export, AI, Telegram, Routes)

---

*Последна актуализация: Декември 2024*

